services:
  mi-app:
    container_name: bulc-perros
    build: .
    image: bulc-perros:v0
    ports:
      - 80:80
    expose:
      - 80
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge